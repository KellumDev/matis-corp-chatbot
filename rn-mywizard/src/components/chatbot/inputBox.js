import React from 'react';

const inputBox = ( ) => {
    return (

        <div style={{
             marginTop: '70%',
            borderWidth: 1
        }} className="inputText">
             <input type="text" />
        </div>

    );
};

export default inputBox;
